News

= form_for [:admin, @news] do |f|
  - if @news.errors.any?
    %div#error_explanation
      %h2
        = pluralize(@news.errors.count, "error")
        prohibited
      %ul
        - @news.errors.full_messages.each do |msg|
          %li= msg
  = f.label :headline
  = f.text_field :headline
  = f.label :subline
  = f.text_field :subline
  = f.label :news_text
  = f.text_area :news_text
  = f.submit "Anlegen"
